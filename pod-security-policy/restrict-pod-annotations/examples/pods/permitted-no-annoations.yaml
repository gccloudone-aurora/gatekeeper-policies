# Test case: Pod with no annotations
# Assert: 
#   namespace purpose: solution
#   Expected: NO VIOLATION (no annotations present)
apiVersion: v1
kind: Pod
metadata:  
  name: test-pod
  namespace: deltaquadrant
  labels:
    app.kubernetes.io/instance: delta-webapp

